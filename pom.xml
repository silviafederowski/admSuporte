<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>br.com.silsys</groupId>
    <artifactId>admSuporte</artifactId>
    <version>1.0.0</version>
    <packaging>war</packaging>

    <name>admSuporte</name>
    <description>
        Aplicativo web de autenticacao (login, cadastro, recuperacao de senha e menu)
        para o condominio, equivalente web do app mobile "Inicio", em Java EE 7 Web
        para GlassFish 4.1, restrito ao dominio silsys.com.br.
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <failOnMissingWebXml>false</failOnMissingWebXml>
    </properties>

    <dependencies>
        <!-- Java EE 7 Web Profile: fornecido pelo GlassFish 4.1 em tempo de execucao -->
        <dependency>
            <groupId>javax</groupId>
            <artifactId>javaee-web-api</artifactId>
            <version>7.0</version>
            <scope>provided</scope>
        </dependency>

        <!--
            Driver JDBC do MySQL: vai empacotado em WEB-INF/lib dentro do proprio WAR.
            A aplicacao abre as conexoes diretamente via DriverManager (ver
            ConnectionProvider), sem depender de nenhum recurso/pool configurado no
            dominio do GlassFish, ja que esta hospedagem so permite deploy via
            autodeploy por FTP, sem acesso a asadmin/console/lib/ext.
        -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>8.0.28</version>
            <scope>compile</scope>
        </dependency>

        <!-- Le a planilha .xlsx "Form" enviada na tela "Formulario Google" -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.2.5</version>
            <scope>compile</scope>
        </dependency>

        <!-- Gera o PDF com os dados do condominio a partir das linhas selecionadas -->
        <dependency>
            <groupId>com.github.librepdf</groupId>
            <artifactId>openpdf</artifactId>
            <version>1.3.30</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>admSuporte</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.3.2</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
